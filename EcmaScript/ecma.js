
const valueArray = [1, 2, 3, 4, 5, 6, 7, 8]

const mapped = valueArray.map((item) => item + "a")
// console.log(mapped)

// const claculateLoan = (principal, rate, year) => {
//    let months = 12;
//    const intrestAmount = (principal / months) * rate * (months * year)
//    const payableAmt = principal + intrestAmount;
//    const emi = payableAmt / (months * year)
//    return ("PayableAmount: " + payableAmt + " IntrestAmount " + intrestAmount.toFixed(2) + " EMI: " + emi.toFixed(2))
// }
// console.log(claculateLoan(300060, 0.1, 2));


// const claculate = (principal, rate, year) => {
//    let months = 12;
//    const intrestAmount = (principal / months) * rate * (months * year)
//    const payableAmt = principal + intrestAmount
//    const emi = payableAmt / (months * year)
//    return ('PayableAmount ' + payableAmt + ' intrestAmount: ' + intrestAmount.toFixed(2) + ' EMI: ' + emi.toFixed(2))
// }
// console.log(claculate(10000, 0.1, 2));


// const calLoan = (principal, rate, year) => {
//    let months = 12
//    const rateofIntrest = (principal / months) * rate * (months * year)
//    const PayableAmount = principal + rateofIntrest
//    const emi = PayableAmount / (months * year)
//    return ("PaybleAmount: " + PayableAmount + " Rate of intrest: " + rateofIntrest.toFixed(2) + " EMI: " + emi.toFixed(2))
// }
// console.log(calLoan(10000, 0.2, 2));






